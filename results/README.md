# Results Structure

This folder contains the final benchmark outputs for the thesis project.

- `playwright/`: raw CSV measurements for Playwright runs
- `cypress/`: raw CSV measurements for Cypress runs
- `summaries/`: aggregated comparison CSV files

The 30-run benchmark files are the final reference outputs for reporting.
